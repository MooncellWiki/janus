# Application logging configuration
[logger]
enable = true
level = "debug"
format = "compact"
# override_filter = "trace"  # Uncomment to override filters

# Web server configuration
[server]
binding = "0.0.0.0"
port = 25150
host = "http://localhost"

# Mailer Configuration
# [mailer]
# host = "smtp.qiye.aliyun.com"
# port = 465
# from_email = "support@mooncell.wiki"
# to_email = "1677759063@qq.com"
# frontend_url = "http://localhost:25150/"
# auth.user = "support@mooncell.wiki"  # Uncomment and add SMTP user
# auth.password = "Mooncell70080190"  # Uncomment and add SMTP password

# Database Configuration
[database]
  uri = "postgres://janus:janus@localhost:25432/janus"

# Bilibili Configuration
[bilibili]
sessdata = "your_bilibili_sessdata_cookie"
bili_jct = "your_bilibili_bili_jct"

# JWT Configuration (required for API authentication)
# Generate ES256 key pair (compatible with jsonwebtoken):
#   openssl ecparam -genkey -name prime256v1 -noout -out private.pem
# Extract public key:
#   openssl ec -in private.pem -pubout -out public.pem
[jwt]
private_key = """-----BEGIN EC PRIVATE KEY-----
YOUR_PRIVATE_KEY_HERE
-----END EC PRIVATE KEY-----"""
public_key = """-----BEGIN PUBLIC KEY-----
YOUR_PUBLIC_KEY_HERE
-----END PUBLIC KEY-----"""

# [sentry]
# dsn = ""
# traces_sample_rate = 1.0
